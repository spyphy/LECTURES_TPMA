\lstset{ %
	language=C++,    % язык кода по умолчанию
	morekeywords={is, as, object, null, byte, sbyte, ulong, decimal, string, interface, delegate, event, add, remove, in, foreach, params, ref, out, get, set, value, finally, this, base, override, ...}, % если хотите добавить ключевые слова, то добавл€йте
	% ÷вета
	keywordstyle=\color{keyword}\ttfamily\bfseries,
	%stringstyle=\color{string}\ttfamily,
	stringstyle=\ttfamily\color{red!50!brown},
	commentstyle=\color{comment}\ttfamily\itshape,
	morecomment=[l][\color{morecomment}]{\#}, 	
	% Ќастройки отображени€  		
	%basicstyle=\small\sffamily, % размер и начертание шрифта дл€ подсветки кода
	basicstyle=\ttfamily, % Ўрифт дл€ отображени€ кода (\footnotesize)
	numbers=left,             % где поставить нумерацию строк (слева\справа)
	numberstyle=\tiny,        % размер шрифта дл€ номеров строк
	stepnumber=1,             % размер шага между двум€ номерами строк
	numbersep=5pt,            % как далеко отсто€т номера строк от подсвечиваемого кода
	backgroundcolor=\color{white}, % цвет фона подсветки - \usepackage{color}
	showspaces=false,        % показывать или нет пробелы специальными отступами
	showstringspaces=false,  % показывать или нет пробелы в строках
	showtabs=false,          % показывать или нет табул€цию в строках
	frame=single,            % рисовать рамку вокруг кода
	%frame=lrb,xleftmargin=\fboxsep,xrightmargin=-\fboxsep, % –амка, подогнанна€ к заголовку
	numberstyle=\footnotesize\color{gray}, % —тиль написани€ номеров строк
	rulecolor=\color{gray}, % ÷вет рамки	
	%emph={int,char,double,float,unsigned,void,bool}
	tabsize=2,               % размер табул€ции по умолчанию равен 2 пробелам
	captionpos=t,            % позици€ заголовка вверху [t] или внизу [b] 
	breaklines=true,         % автоматически переносить строки (да\нет)
	breakatwhitespace=false, % переносить строки только если есть пробел
	escapeinside={\%*}{*)}   % если нужно добавить комментарии в коде
}

%\lstset{style=sharpc}
